import React from "react";
import Logo from "../assets/img/Logo.svg";

const Nav = () => {
  return (
    <div>
      <img src={Logo} alt="Logo for the restaurant" />
    </div>
  );
};

export default Nav;
